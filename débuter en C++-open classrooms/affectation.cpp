#include <iostream>

// using namespace std;

int main()
{
    int a = 0, b = 3;
    // char tab[10];
    // int  resultat = 0;
    // resultat = a + b;
    if(a)
        std::cout << "La valeur de la variable a c'est: " << a << std::endl;
    else
        std::cout << "La valeur de la variable a n'existe pas "<< std::endl;
    return 0;
}